<section class="user-card-cont">
    <img src= <%= user.photo %> alt= '<%= user.name + ' photo'%>'/>
    <div>
      <h1><%= user.name %></h1>
      <h3>Number of posts: <%= user.posts_counter %></h3>
    </div>
</section>
